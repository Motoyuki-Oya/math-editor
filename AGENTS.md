# 作業ルール

## 承認の範囲

- 質問に答えている段階では実装しない。回答の内容によって設計が変わりうる間は、関連するバグ修正にも着手しない。
- 「進めていい」は、直前に明示して列挙した項目だけへの承認として扱う。列挙していない作業は、対象を明示して確認してから始める。
- 過渡的な実装を入れるときは、それが最終形のどの部品になるか（何が残り、何が捨てられるか）を先に示し、最終形と無関係な「つなぎ」を独断で積まない。
- ユーザーが示した設計方針を、自分で作った段階分けや難易度の棚（「これは将来の級」）で先送りしない。段階を切るなら、各段が指示された最終形へ向かうことを示して合意を得る。
- 他エディタの実装調査は参考情報であり、指示された要件を上書きする根拠にしない。
- 違反の記録（繰り返さないため）:
  - 2026-08、設計質問への回答だけを求められている段階で、再設計で扱うと合意していた Undo バグへ局所修正を実装し、差し戻された。
  - 2026-08、「行は随時読めばいい」という設計指示に対し、本体ストアを全文 RAM 読みで実装して seek 方式を「数十GB級」として独断で先送りし、Zed の全読み実装を根拠に正当化した。

## 製品コンセプト

- Planetext は数式エディタではない。普通のテキストエディタに、プレーンテキストでは表現できない二次元の配置だけを追加するエディタである。
- 分数、ルート、上下配置、行列などは数学的な意味ではなく、表示上の形を作る構造として扱う。
- 構造内部の文字も通常の文字である。数、変数、識別子、演算子、関係記号、関数などの数学的・構文的な分類を行わない。
- 文字種に応じた数学的な間隔、自動組版、数式構文解析、暗黙の意味付けを実装しない。文字の見た目や動作は、構造の内外で可能な限り同じにする。
- `Math`、`math`、`数式`などの既存の内部名やコメントは歴史的な名称であり、数学的意味を追加してよい根拠にしない。
- 文書の改行は `Text.lines` だけが持つ。構造内部の `Row` に改行を追加しない。構造編集中の Enter は構造を抜ける操作とする。
- 図、グラフ、一般的な表、テキストボックス、枠付き文章、装飾レイアウトなど、見た目を整える機能は別ツールの役割であり実装しない。構造は、普通のテキストでは表現できない配置そのものに必要な最小限へ限定する。

## 実装時

- 実装を行う際はまず全体のアーキテクチャ(architecture.md)を念頭に置くこと
- コミット単位は意味のある最小単位に分けて実施すること　1機能1コミット
- 修正においては追記追記ではなく全体を置き換えられないか？コード量が減らせないか？を検討する
- 簡単な問題を複雑な仕組みに置き換えない。既に動いている配置や処理を維持し、問題のある軸・責務だけを最小変更で直す
- 1つの問題へ責務外の仕組みや補正を重ねない。修正量が増え始めた場合は実装を止め、追加コードではなく単純化によって解決できる設計へ戻る
- 機能、抽象、データ構造、API、外部技術を、本来の目的や責務と異なる用途へ流用しない。偶然得られる副作用や未保証の挙動へ依存せず、必要な責務はそれを表す仕組みとして実装する
- 二次元構造の縦位置は、Viewで子構造の高さと基準軸を再帰的に計算して決める。ブラウザーの暗黙のbaselineへ委ねない
- 作業中に製品コンセプトやアーキテクチャへ反する既存実装を見つけた場合、無視したり、その実装を前提に回避策を重ねたりしない。変更対象との関係、影響範囲、置き換え方針を明示し、依頼範囲を超える修正が必要なら実装前に確認する
- コンセプト違反が修正対象の根本原因である場合は、局所的に症状を隠さず、違反している構造そのものを修正対象として扱う
- コメントはWhatは不要WhyやHowを書くこと。ただし複雑である場合はWhatも記載する

## 修正時の検証

- 問題を修正するとき、機能していない状態を前提に作成したテストの成功だけを、修正完了の根拠にしない。
- 修正対象そのものは、実際の再現手順と、その機能が動作する実行環境で確認する。表示、折り返し、キャレット、IME、DOM 計測などブラウザー／WebView に依存する問題は、実 DOM または GUI で検証する。
- 単体テストで確認できるのは、モデルや補助関数など、そのテストが直接観測している範囲だけとする。単体テストが通っていても、実際の不具合が再現する場合は未修正として扱う。
- リグレッションテストは、修正対象外の既存機能へ影響がないことを確認するために使う。修正対象の動作確認をリグレッションテストだけで代替しない。
- 完了報告では、修正対象をどの実環境・再現手順で確認したかと、リグレッションテストで確認した範囲を分けて記載する。
