# 作業ルール

## 承認の範囲

- 質問に答えている段階では実装しない。回答の内容によって設計が変わりうる間は、関連するバグ修正にも着手しない。
- 「進めていい」は、直前に明示して列挙した項目だけへの承認として扱う。列挙していない作業は、対象を明示して確認してから始める。
- 過渡的な実装を入れるときは、それが最終形のどの部品になるか（何が残り、何が捨てられるか）を先に示し、最終形と無関係な「つなぎ」を独断で積まない。
- ユーザーが示した設計方針を、自分で作った段階分けや難易度の棚（「これは将来の級」）で先送りしない。段階を切るなら、各段が指示された最終形へ向かうことを示して合意を得る。
- 他エディタの実装調査は参考情報であり、指示された要件を上書きする根拠にしない。

## 設計段階のルール

- 設計・アーキテクチャの議論中は、実装コード（`src/`、`src-tauri/`、`tests/`、`Cargo.toml` 等）を一切変更しない。変更してよいのは合意したドキュメントだけ。
- 設計対象として合意した範囲以外の設計を勝手に広げない。境界の議論中に内部の再設計（MVC、同期方式、検索など）を持ち込まない。
- `docs/architecture.md` には合意した ToBe だけを書く。AsIs とのギャップや移行手順は `docs/refactoring-plan.md` に書き、混ぜない。
- 未合意の案は「未確定」と明示して分離し、確定事項のように書かない。
- 図は Mermaid を使い、単純な境界を複雑な図にしない。
- 「点検して」「検討して」は調査と報告の依頼であり、実装や書き換えの許可ではない。
- 違反の記録（繰り返さないため）:
  - 2026-08、設計質問への回答だけを求められている段階で、再設計で扱うと合意していた Undo バグへ局所修正を実装し、差し戻された。
  - 2026-08、「行は随時読めばいい」という設計指示に対し、本体ストアを全文 RAM 読みで実装して seek 方式を「数十GB級」として独断で先送りし、Zed の全読み実装を根拠に正当化した。
  - 2026-08、根号の鉤と上線が別要素で描かれている設計不整合に対し、1つのSVGへ統合するあるべき構造を先に選ばず、幅の重なりと線幅調整で接続を補正しようとした。

- 成功パターンの記録（再現性を高めるため）:
  - 悪かった時の記録だけでなく、**良かった時のアプローチや設計思考、ユーザーに評価された対応**を記録し、再現性を高める。ユーザーから褒められた際は、その理由・アプローチ・設計判断を必ずここに記録・蓄積すること。
  - 2026-09、下書き復元後の Undo 時に変更行ハイライトが戻らない問題に対し、「Clean に戻った時だけ消す」という場当たりな局所補正を退け、「`architecture.md` の大原則（ログが単一の真実）に立ち返り、未保存ログ（`unsaved_transactions`）から各 Undo ステップごとの変更行（`modified_lines`）を厳密に導出する」という本質的設計を提示。多段階 Undo による段階的ハイライト巻き戻しと Clean 復帰を完璧に実現し、「完璧な設計」として高く評価された。
  - 2026-09、復元後の Undo が 1 文字ずつ細切れになる問題に対し、下書き差分に `group` を保持させて元の `Transaction`（キーストロークのまとまり）を復元時にも再結合する設計を適用。プロセス終了前と同一の Undo 単位を完全復元し、「完璧な動作。これ実現できてるエディタ少ない」と高く評価された。
  - 2026-09、ベンチマークが単体 API だけを測り実機 GUI 起動パイプライン（`read_drafts` -> `open_draft`）を測っていなかったため、実機フリーズを見逃した失敗から、「ベンチマークと実機の動作フローを完全に一致させる（E2E パイプライン計測）」原則を確立。同時に「10MB超分岐」のような場当たり対症療法を完全撤廃し、下書きに削除行テキストを持たせる自己完結化により、復元時のディスクアクセス・走査待機を根絶。800MB 規模でも 46ms の高速復元を実現した。
  - 2026-09、「Clean に戻した後に下書きを消すと Redo も消えてしまう」というユーザーの鋭い指摘から、Undo された操作も含めて全未保存トランザクションと `head` 相対位置を下書きに記録する設計を導入。Clean に戻した状態での再起動であっても、復元後に Redo（Ctrl+Y）で元の編集へどこまでも進める完全なタイムトラベル復元を実現した。

## 製品コンセプト

- Planetext は数式エディタではない。普通のテキストエディタに、プレーンテキストでは表現できない二次元の配置だけを追加するエディタである。
- 分数、ルート、上下配置、行列などは数学的な意味ではなく、表示上の形を作る構造として扱う。
- 構造内部の文字も通常の文字である。数、変数、識別子、演算子、関係記号、関数などの数学的・構文的な分類を行わない。
- 文字種に応じた数学的な間隔、自動組版、数式構文解析、暗黙の意味付けを実装しない。文字の見た目や動作は、構造の内外で可能な限り同じにする。
- `Math`、`math`、`数式`などの既存の内部名やコメントは歴史的な名称であり、数学的意味を追加してよい根拠にしない。
- 文書の改行は `Text.lines` だけが持つ。構造内部の `Row` に改行を追加しない。構造編集中の Enter は構造を抜ける操作とする。
- 図、グラフ、一般的な表、テキストボックス、枠付き文章、装飾レイアウトなど、見た目を整える機能は別ツールの役割であり実装しない。構造は、普通のテキストでは表現できない配置そのものに必要な最小限へ限定する。

## 不具合発見時
### 不具合対応時の「3ステップ強制停止プロトコル」の義務化
ユーザーからの不具合報告、または実装中の調査で問題・バグを発見した際は、いかなるファイル編集ツールも呼ぶ前に、直ちに以下の手順を順守します。
-Step 1（即時停止・事実報告）:
原因を発見した瞬間にコード変更を行わず、「何が原因で何が起きていたのか」をまずユーザーへ正確に報告する。
-Step 2（横展開の徹底点検）:
同一・類似の不適切な前提（今回は「巨大ファイルだからと機能を間引く古い分岐」）が他モジュールやUI/コア間に残っていないかを必ず網羅的に横展開調査し、その点検結果を Step 1 と合わせて報告する。
-Step 3（合意後の是正実行）:
修正範囲（局所修正だけでなく横展開で判明した関連コードの引き算・根絶）についてユーザーの承認を得てから、はじめてコードの修正に着手する。

## 実装時

- 合意したアーキテクチャ(`docs/architecture.md`)と `docs/refactoring-plan.md` のチェック項目を逸脱しない。
- 実行する前に不明点を確認する。確認の中で既存実装や指示がアーキテクチャに反していると気づいたら、実装せずに指摘する。
- 全てがクリアになったら実装を開始し、合意したステップ(refactoring-plan.md の移行順序の 1 ステップ)が終わるまで完遂する。完遂は「中途半端にやめない」の意味であり、途中で不明点・アーキテクチャ違反・設計の綻びが出たら止めて確認することを優先し、解消後に完遂へ戻る。ステップ内で列挙外の作業が必要になった場合も、止めて確認してから進める。
- 実装を行う際はまず全体のアーキテクチャ(architecture.md)を念頭に置くこと
- コミット単位は意味のある最小単位に分けて実施すること　1機能1コミット
- 修正においては追記追記ではなく全体を置き換えられないか？コード量が減らせないか？を検討する
- 実装前に、修正対象が本来どの責務・データ・描画要素に所有されるべきかを定義し、「あるべき最終構造」と局所補正案を比較する。最終構造へ直接直せる場合は局所補正案を採用しない
- 「最小変更」は最小差分を意味しない。誤った責務境界を残す小さなパッチではなく、問題の責務を完結して正しくする最小単位を選ぶ
- 変更量は追加行だけでなく、追加量から削除量を差し引いた正味と、変更後に残る総コード量で評価する。追加が多くても、それ以上の重複・補正・誤った実装を削除できる案を優先する
- 1つの意味または見た目を表すものを、別々の要素・別々の座標系・別々の状態へ分けたまま、オフセット、余白、重なり、色や太さの上書き、タイマー等で接続してはならない。1つの所有者・要素・計算へ統合する
- マジックナンバーや微調整で要素同士を一致させる必要が出た時点で実装を止め、同じ値を共有するのではなく、同じ責務へ統合できないか再設計する
- 簡単な問題を複雑な仕組みに置き換えない。既に動いている配置や処理を維持し、問題のある軸・責務だけを最小変更で直す
- 1つの問題へ責務外の仕組みや補正を重ねない。修正量が増え始めた場合は実装を止め、追加コードではなく単純化によって解決できる設計へ戻る
- 機能、抽象、データ構造、API、外部技術を、本来の目的や責務と異なる用途へ流用しない。偶然得られる副作用や未保証の挙動へ依存せず、必要な責務はそれを表す仕組みとして実装する
- 二次元構造の縦位置は、Viewで子構造の高さと基準軸を再帰的に計算して決める。ブラウザーの暗黙のbaselineへ委ねない
- 作業中に製品コンセプトやアーキテクチャへ反する既存実装を見つけた場合、無視したり、その実装を前提に回避策を重ねたりしない。変更対象との関係、影響範囲、置き換え方針を明示し、依頼範囲を超える修正が必要なら実装前に確認する
- コンセプト違反が修正対象の根本原因である場合は、局所的に症状を隠さず、違反している構造そのものを修正対象として扱う
- コメントはWhatは不要WhyやHowを書くこと。ただし複雑である場合はWhatも記載する

## 修正時の検証

- Windows上でPlanetextのGUI、WebView2、IME、RTL、キャレット、ネイティブファイルダイアログを検証するときは、`.devin/skills/testing-planetext-windows/SKILL.md` を最初に呼び出し、HWNDベースの手順を使う。
- 問題を修正するとき、機能していない状態を前提に作成したテストの成功だけを、修正完了の根拠にしない。
- 新機能を作成したときは、その新機能に対するテストを必ず作成する。
- 修正対象そのものは、実際の再現手順と、その機能が動作する実行環境で確認する。表示、折り返し、キャレット、IME、DOM 計測などブラウザー／WebView に依存する問題は、実 DOM または GUI で検証する。
- ただし突然実行しない。ユーザーの操作とバッティングするため、**実施直前**に確認をいれること。
- 単体テストで確認できるのは、モデルや補助関数など、そのテストが直接観測している範囲だけとする。単体テストが通っていても、実際の不具合が再現する場合は未修正として扱う。
- リグレッションテストは、修正対象外の既存機能へ影響がないことを確認するために使う。修正対象の動作確認をリグレッションテストだけで代替しない。
- 完了報告では、修正対象をどの実環境・再現手順で確認したかと、リグレッションテストで確認した範囲を分けて記載する。

## コミット・プルリクエスト作成時

- 日本語でかく。
- コミットメッセージに具体的なファイル名や関数名は不要。
- 変更の目的と影響を簡潔に記述する。変更の意図を明確にする。
- プルリクエストは、修正対象の問題を解決することを目的としていることを明確に記述する。変更の背景、影響範囲、テスト結果を含める。
